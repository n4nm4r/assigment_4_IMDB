<UserControl x:Class="assigment_4_IMDB.Views.SeriesDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:assigment_4_IMDB.Views"
             xmlns:vm="clr-namespace:assigment_4_IMDB.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <local:StarConverter x:Key="StarConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid Margin="20">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0">
                <!-- Title -->
                <TextBlock Text="{Binding PrimaryTitle}" FontSize="24" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="{Binding StartYear, FallbackValue='N/A'}" TextAlignment="Center"/>

                <!-- Number of Episodes -->
                <TextBlock Text="{Binding EpisodeParentTitles.Count, StringFormat='Episodes: {0}'}" FontSize="16" Margin="0,10,0,10" />

                <!-- Rating -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding Rating.AverageRating, StringFormat=Rating: {0:F1}}" FontSize="16" Margin="0,0,10,0"/>
                    <ItemsControl ItemsSource="{Binding Rating.AverageRating, Converter={StaticResource StarConverter}}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="★" FontSize="18" Foreground="Gold" Margin="1"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <!-- Genres -->
                <TextBlock Text="Genres:" FontSize="16" FontWeight="SemiBold" Margin="0,20,0,5"/>
                <ItemsControl ItemsSource="{Binding Genres}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" FontSize="14" Margin="2"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
